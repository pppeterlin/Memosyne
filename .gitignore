# ══════════════════════════════════════════
#  秘鑰與憑證（絕對不進 git）
# ══════════════════════════════════════════
openrouter-key
openrouter.py
ANTHROPIC_API_KEY
*.key
.env
.env.*

# ══════════════════════════════════════════
#  個人記憶 — 2026-04-23 起改由 Personal_Brain_DB/_vault submodule 追蹤
#  原路徑 10_Profile ~ 50_Knowledge 現為 symlink 指向 _vault/，主 repo 只記 symlink
# ══════════════════════════════════════════

# ── 原始資料來源 ──
gemini\ chat/
notes/
個人記憶開發文檔.md

# ── spring/ 記憶之泉（原始檔案與已處理歸檔不進 git）──
spring/_processed/
# 個人檔案（.pages, .txt）也不追蹤，但 README.md 和 .gitkeep 保留
spring/*.pages
spring/*.txt

# ══════════════════════════════════════════
#  系統生成／大型二進位（不 commit）
# ══════════════════════════════════════════

# 向量資料庫
Personal_Brain_DB/00_System/chroma_db/

# FlashRank / HuggingFace 模型快取
Personal_Brain_DB/00_System/flashrank_cache/

# 以下檔案 2026-04-23 後改由 _vault submodule 管理；00_System/ 下保留 symlink 作為 shim
# （symlink 本身會被追蹤，指向 ../_vault/；私有內容在 private repo）
# - index.json, bm25_index.pkl, muse_centroids.json（索引 / centroid）
# - tapestry.json, tapestry_db（圖譜）
# - hyqe_cache.json, contextual_cache.json（LLM 產出快取）
# - chronicle.db（存取紀錄）
# - augury_reports/（Augury 審計）
# - benchmark/reports/（Eternal Mirror 評估報告）

# ══════════════════════════════════════════
#  Python 暫存
# ══════════════════════════════════════════
**/__pycache__/
**/*.pyc
**/*.pyo
.Python

# ══════════════════════════════════════════
#  系統雜項
# ══════════════════════════════════════════
.DS_Store
.AppleDouble
**/.DS_Store
.claude/
/_internal/
**/.cursorrules
.env
